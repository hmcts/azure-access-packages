#database access approver required 3 days timeout for CFT DB Sandbox Demo & DB  Sandbox Demo
name: "Database-Approver"
policy:
  display_name: "Initial Policy"
  description: "Initial Policy"
  duration_in_days: 1
  approval_settings:
    approval_required_for_extension: true
    approval_required: true
    approval_stage:
      approval_timeout_in_days: 3
      approver_justification_required: true
      enable_alternative_approval_in_days: 0
      primary_approver:
        subject_type: "groupMembers"
      alternative_approver:
        subject_type: "groupMembers"
    requestor_justification_required: true
  assignment_review_settings:
    access_recommendation_enabled: true
    access_review_timeout_behavior: "keepAccess"
    approver_justification_required: true
    duration_in_days: 14
    enabled: false
    review_frequency: "annual"
    review_type: "Reviewers"
    reviewer:
      backup: true
      subject_type: "groupMembers"
  extension_enabled: true
  questions:
  - required: true
    sequence: 1
    text:
      default_text: "Include JIRA link in business justification"
  requestor_settings:
    requestor:
      subject_type: "groupMembers"
    requests_accepted: true
    scope_type: "SpecificDirectorySubjects"
